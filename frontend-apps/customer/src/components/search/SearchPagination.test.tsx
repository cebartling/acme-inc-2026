import { describe, it, expect } from "vitest";
import { SearchPagination } from "./SearchPagination";

describe("SearchPagination", () => {
  it("is defined", () => {
    expect(SearchPagination).toBeDefined();
  });
});
