import { describe, it, expect } from "vitest";
import { SearchSortSelector } from "./SearchSortSelector";

describe("SearchSortSelector", () => {
  it("is defined", () => {
    expect(SearchSortSelector).toBeDefined();
  });
});
